# `opengrow.twin` Kit extension

This directory is reserved for the Omniverse Kit UI/controller that invokes the stable `opengrow simulate` CLI and loads its result assets. The scientific engine must remain independently executable and testable.
